# Backend Environment Variables
# Copy to backend/.env and adjust as needed.

PORT=3000
NODE_ENV=development

# PostgreSQL connection (matches scripts/bootstrap_sim_env defaults)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=skills_inventory
DB_USER=sim_user
DB_PASSWORD=SimP@ssw0rd

# JWT configuration
JWT_SECRET=super-secret-key
JWT_EXPIRES_IN=24h

# API base configuration
API_VERSION=v1
API_BASE_URL=/api
CORS_ORIGIN=http://localhost:5173
LOG_LEVEL=info
